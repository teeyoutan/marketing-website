---
layout: page.hbs
visible_title: A/B Test Sample Size Calculator
body_class:
- sample-size-page
page_script: sample-size-calculator.js
---

{{#with sample_size_calculator.calculator_content}}
<div class="callout calc-wrapper">

  {{#each conversion}}
  <div class="row">
    <div class="columns one-third calc-percents">
      <label for="" class="calc-demo-title">{{demo}}</label>
      <div class="input-wrapper">
        <input type="text">
        <span>%</span>
      </div>
    </div>
    <div class="columns two-thirds">
      <p class="calc-content">{{description}}</p>
    </div>
  </div>
  {{/each}}

  {{#each statistics}}
  <div class="row">
    <div class="columns one-third">
      <label for="" class="calc-demo-title">{{demo}}</label>
      <div class="calculator-variables">80%</div>
      <button class="button-link">{{button_text}}</button>
    </div>
    <div class="columns two-thirds">
      <p class="calc-content">{{description}}</p>
    </div>
  </div>
  {{/each}}

  {{#with tailed}}
  <div class="row">
    <div class="columns one-third">
      {{#each demo}}
      <div class="tails">
        <input type="radio">
        <label for="">
          {{this}}
        </label>
      </div>
      {{/each}}
    </div>
    <div class="columns two-thirds">
      <p class="calc-content last">{{description}}</p>
    </div>
  </div>
  {{/with}}
  
  <div class="calc-sample-cont">
    <h3>{{bottom_title}}</h3>
    <div id="sample">10,000</div>
  </div>
  

</div>
{{/with}}

<div class="calc-faq-wrapper">
  {{#with sample_size_calculator.faq_content}}
  <h3>{{title}}</h3>
  <div class="calc-faq-cont">
    {{#each sections}}
    <div class="calc-faq-subtitle">{{sub_title}}</div>
    <p>
      {{description}}
      {{#if link}}
      <a href="{{link.path}}" target="_blank">{{link.title}}</a>
      {{/if}}
    </p>
    {{/each}}
  </div> 
  {{/with}}

  {{#with sample_size_calculator.cta}}
  <div class="callout">
    <h4>{{title}}</h4>
    <a href="{{button.path}}" target="_blank" class="primary-button">{{button.text}}</a>
  </div>
  {{/with}}
</div>